$(document).ready(function(){
  $(".question-box").submit(function() {
    var co_size = parseint($("input#co_size").val());
    var platform = parseint($("input#platform").val());
    var stack = parseint($("input#stack").val());
    var city = parseint($("input#city").val());
  });
});
